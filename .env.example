# Kütüphane Yönetim Sistemi - Ortam Değişkenleri
# Bu dosyayı .env olarak kopyalayın ve değerleri düzenleyin

# JWT Secret Key (ÖNEMLİ: Production'da güçlü bir key kullanın!)
# Örnek güçlü key oluşturma: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your-secret-key-here-change-in-production

# Veritabanı Yapılandırması
DB_USER=library_user
DB_PASSWORD=library123
DB_HOST=localhost
DB_PORT=5432
DB_NAME=library_db

# CORS Yapılandırması
# Production'da izin verilen domain'leri virgülle ayırarak yazın
# Örnek: https://example.com,https://app.example.com
CORS_ORIGINS=http://localhost:3000,http://localhost:5000

# Uygulama Ortamı (development/production)
FLASK_ENV=development

# HTTPS Zorunluluğu (production için True yapın)
FORCE_HTTPS=False
